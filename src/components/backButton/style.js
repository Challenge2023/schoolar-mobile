import { StyleSheet } from "react-native";

export default StyleSheet.create({
  button: {
    width: 50,
    height: 50,
    display: "flex",
    alignItems: 'center',
    justifyContent: 'center',
    borderRadius: 10,
  },
});
