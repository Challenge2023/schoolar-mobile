import { StyleSheet } from "react-native";

export default StyleSheet.create({
    space: {
        display: 'flex',
        gap: 10
    },
    containerForm: {
        display: 'flex',
        gap: 40,
        paddingBottom: 60
    }

});